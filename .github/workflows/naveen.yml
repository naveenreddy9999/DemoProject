name: naveen_job
on:
  workflow_dispatch:
 schedule:
  - cron: '* * * * *'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run a one-line script
        run: java HelloWorld.java


